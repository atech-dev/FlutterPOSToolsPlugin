name: flutter_pos_tools
description: A plugin that provides tools to work with POS devices
version: 0.1.0
homepage:

environment:
  sdk: '>=2.18.4 <3.0.0'
  flutter: ">=2.5.0"

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  plugin:
    platforms:
      android:
        package: ao.co.atech.dev.flutter_pos_tools
        pluginClass: FlutterPosToolsPlugin
